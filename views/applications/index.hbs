<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/dashboard.css">
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand">Job Tracker</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/job-list">Job Search</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/applications">Applications</a>
          </li>
          <li class="nav-item">
              <form action="/logout" method="get">
                <button type="submit" class="btn btn-link btn-danger ">Logout</button>
              </form>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container-body">
    <h1 class="dashboard-header">Job Applications</h1>

    <!-- Add New Application Button -->
    <a href="/applications/new" class="btn btn-primary mb-3 application-btn">Add New Application</a>

    <!-- Search Bar -->
    <div class="input-group mb-3">
      <input type="text" class="form-control" id="searchInput" placeholder="Search by job title, company..." onkeyup="searchApplications()">
      <button class="btn btn-outline-secondary" type="button">Search</button>
    </div>

    <!-- Applications List -->
    <div class="list-group">
      {{#each applications}}
        <div class="list-group-item d-flex justify-content-between align-items-center">
          <div>
            <h5>{{jobTitle}} at {{company}}</h5>
            <p class="text-muted">{{location}} | {{formatDate applicationDate 'MMMM Do, YYYY'}}</p>
          </div>
          <div>
            <a href="/applications/{{_id}}/edit" class="btn btn-warning btn-sm">Edit</a>
            <form action="/applications/{{_id}}/delete" method="POST" style="display: inline;">
              <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this application?')">Delete</button>
            </form>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
</body>
</html>